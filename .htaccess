# Enable rewriting (important if your app uses clean URLs)
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /abaya-desginer/

    # Example: redirect all requests to index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>

# PHP settings (use php.ini instead of .htaccess for XAMPP)
